# ビルドステージ: 必要なバイナリを取得
FROM busybox:1.35.0-uclibc AS bin

# 実行ステージ: Distroless イメージに必要なバイナリをコピー
FROM gcr.io/distroless/base:nonroot AS runner

# 必要なコマンドをコピー
COPY --from=bin /bin/sh /bin/sh
COPY --from=bin /bin/ls /bin/ls
COPY --from=bin /bin/cat /bin/cat

# エントリーポイントを設定
ENTRYPOINT ["/bin/sh"]
